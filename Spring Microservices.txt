1. make 4 copies
2. change the tablescript.. make schema and correstion... remove forignkey constraint
3. application.properties file... name,port, url
4. service... kept the relevant serivce
5. entitiy package... remove the unneccessary enties

mysql error if we get.
mysqld --defaults-file=C:\my.ini
   --initialize --console

GitHub:
https://infygithub.ad.infosys.com/
Username and passw is infosys username and passw

Load Balancing(Ribbion):
To generate port automatically
${PORT:${SERVER_PORT:0}}

Eureka:
 to show 2 instances:
eureka.instance.instance-id=${spring.cloud.client.hostname}:${spring.application.name}:${spring.application.instance_id:${random.value}}
Any error(Last Resort)
<dependency>
    <groupId>javax.xml.bind</groupId>
    <artifactId>jaxb-api</artifactId>
    <version>2.3.1</version>
</dependency>
<!-- https://mvnrepository.com/artifact/org.javassist/javassist -->
<dependency>
    <groupId>org.javassist</groupId>
    <artifactId>javassist</artifactId>
    <version>3.25.0-GA</version>
</dependency>

<dependency>
    <groupId>javax.xml.bind</groupId>
    <artifactId>jaxb-api</artifactId>
    <version>2.3.1</version>
</dependency>
<!-- https://mvnrepository.com/artifact/org.javassist/javassist -->
<dependency>
    <groupId>org.javassist</groupId>
    <artifactId>javassist</artifactId>
    <version>3.25.0-GA</version>
</dependency>